# ===========================================
# RAYMOND ERP - Variables de Entorno
# ===========================================
# Copia este archivo a .env y ajusta los valores según tu entorno

# ===========================================
# Base de Datos PostgreSQL
# ===========================================
DB_USER=Raymond
DB_PASSWORD=tu_password_seguro_aqui
DB_NAME=raymond_db
DB_PORT=5432
DB_HOST=postgres

# URL de conexión completa (formato: postgresql://user:password@host:port/database)
DATABASE_URL=postgresql://raymond:tu_password_seguro_aqui@postgres:5432/raymond_db

# ===========================================
# API (NestJS)
# ===========================================
API_PORT=3000

# JWT Secrets (genera valores seguros, mínimo 32 caracteres)
JWT_SECRET=tu_jwt_secret_super_seguro_minimo_32_caracteres_largo
JWT_REFRESH_SECRET=tu_jwt_refresh_secret_super_seguro_minimo_32_caracteres_largo

# CORS Origin (URL del frontend)
# Puedes especificar múltiples orígenes separados por comas
# Ejemplo: CORS_ORIGIN=https://raymond.runsolutions-services.com,https://app.raymond.com
CORS_ORIGIN=https://raymond.runsolutions-services.com

# Super Admin Emails (separados por comas)
# Usuarios con estos emails tendrán acceso completo sin verificación de permisos
SUPER_ADMIN_EMAILS=j.molina@runsolutions-services.com

# Swagger (solo para desarrollo)
# Establecer a 'true' para habilitar Swagger en producción (NO RECOMENDADO)
ENABLE_SWAGGER=false

# ===========================================
# Web (Next.js)
# ===========================================
WEB_PORT=3001

# URL de la API para el frontend
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ===========================================
# Redis (Opcional, para queues y cache)
# ===========================================
# Si la aplicación corre dentro de Docker, usa:
# REDIS_HOST=redis
# REDIS_PORT=6379
# Si la aplicación corre fuera de Docker (en el host), usa:
REDIS_HOST=localhost
REDIS_PORT=6380

# ===========================================
# Entorno
# ===========================================
NODE_ENV=production

